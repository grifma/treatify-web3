version: 2
jobs:
  build:
    working_directory: ~/repo
    docker:
      - image: circleci/node:10.20.0

    steps:
      - checkout
      - persist_to_workspace:
          root: .
          paths:
            - client
      - run: npm install
      - run: npm run dev
